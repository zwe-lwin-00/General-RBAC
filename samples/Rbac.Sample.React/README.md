# Sample React console

Vite + React + `@general-rbac/react`.

```bash
dotnet run --project ../../samples/Rbac.Sample.Api
npm install
npm run dev
```

The Vite dev server proxies `/api` to `http://localhost:5265`.
